export default function Logo({ className = '' }) {
  return (
    <span
      className={`text-[24px] font-bold tracking-tight text-terra font-display ${className}`}
    >
      ladder.
    </span>
  )
}